# PACUS — Checklist de Segurança e LGPD

Gerado a partir da auditoria de 2026-08-28/29. Regra: seguimos esta lista, um item por vez, na ordem abaixo. Os itens marcados `[x]` foram concluídos inicialmente em `feature/next-migration` e, quando aplicável, posteriormente integrados em `main`.

Convenção:
- **[AQUI]** — dá pra fazer nesta sessão (código, testes, documento que eu redijo).
- **[DEPOIS]** — precisa de acesso externo (Render, MongoDB Atlas, GitHub settings) ou decisão/assinatura sua. Fica anotado aqui pra não esquecer, eu não mexo nisso sozinho.

---

## Fase A — Segurança (Prioridade 1, itens que faltam)

- [x] **[AQUI] A1. Rate limiting no login (adulto e criança) e no bootstrap.** Hoje não existe limite de tentativas — o PIN da criança tem só 4 dígitos (10.000 combinações), então dá pra forçar bruta sem nenhum bloqueio. Implementar limite por IP/usuário nos endpoints `/api/v1/auth/*` e `/api/v1/bootstrap`.
- [x] **[AQUI] A2. Testes de isolamento por família nos controllers que ainda não têm** (Store, Habitat, Points, DailyRoutines, Settings) — hoje só `TasksController` tem esse tipo de teste. Replicar o padrão criado em `TasksHttpIntegrationTests.cs`.
- [x] **[AQUI] A3. Testes de manipulação de ObjectId / troca de papel** (criança tentando ação de adulto, adulto de uma família usando id de outra) nos endpoints que ainda não cobrem isso.
- [x] **[AQUI] A4. Renomear os campos `UserId` que na verdade significam `FamilyId`** em `DailyRoutine`, `TaskTemplate`, `StoreItem`, `Redemption`, `Settings`, `PointTransaction` (entidades + repositórios + serviços). Não é bug hoje, mas é a maior fonte provável de bug futuro.
- [x] **[AQUI] A5. Log de auditoria para ações administrativas sensíveis** (excluir tarefa, aprovar/rejeitar resgate, ajustar pontos manualmente) — registrar quem fez, quando, e o que mudou, separado do dado em si.
- [ ] **[DEPOIS] A6. Confirmar no Render/Atlas:** usuário do MongoDB com privilégio mínimo, rede restrita (IP allowlist), TLS ativo, `CORS_ALLOWED_ORIGINS` configurado só com o domínio real (não `*`), HTTPS redirect funcionando de fato em produção.
- [ ] **[DEPOIS] A7. Checar o histórico do Git em busca de segredo vazado** (procurar se `JWT_SECRET`/connection string do Mongo já apareceram em algum commit antigo) e rotacionar se achar algo.
- [ ] **[DEPOIS] A8. Rotacionar o PAT clássico do GitHub** (`ghp_...`, escopo `repo` completo) por um fine-grained token limitado só a este repositório, ou revogar quando não precisar mais de push automatizado.

## Fase B — LGPD: base (Prioridade 2)

- [x] **[AQUI] B1. Mapa de dados** — documento cobrindo as 11 collections (`users`, `pacus`, `daily_routines`, `task_templates`, `point_transactions`, `pacus_growth`, `task_events`, `habitats`, `settings`, `store_items`, `redemptions`): campo, finalidade, categoria do titular, origem, quem acessa, base de tratamento, retenção, destino em exclusão, controles de segurança. Ver `docs/DATA_MAP.md`.
- [x] **[AQUI] B2. Endpoint de exportação de dados** (adulto exporta os dados da própria família em JSON/CSV). Implementado em JSON (`GET /api/v1/export`); CSV avaliado e descartado — ver nota no log de progresso.
- [x] **[AQUI] B3. Endpoint de exclusão de conta** com estratégia de cascata/anonimização definida por collection (a partir do mapa de dados do B1). Ver log de progresso.
- [x] **[AQUI] B4. Rascunho de Política de Privacidade e Termos de Uso** (documento — você revisa e, se quiser, valida com um advogado antes de publicar). Ver `docs/POLITICA_PRIVACIDADE.md` e `docs/TERMOS_DE_USO.md`.
- [ ] **[DEPOIS] B5. Publicar Política de Privacidade/Termos no site**, decidir onde ficam hospedados e como o usuário aceita (tela de consentimento) — decisão de produto/UX sua.
- [x] **B6. Registro de consentimentos.** O cadastro exige aceite explícito do responsável antes de criar o perfil infantil. O backend registra data/hora e a versão do texto aceito no perfil adulto; o perfil da criança não armazena esse dado.

## Fase C — Crianças (Prioridade 3)

- [x] **[AQUI] C1. Revisão dos campos da collection `users`** — checar se há dado coletado sem necessidade clara e propor remoção. Ver `docs/DATA_MAP.md` (seção 1 e achado #5).
- [x] **[AQUI] C2. Teste específico: criança da Família A não enumera/acessa nada da Família B** (cobertura adicional além do que já existe). Ver log de progresso.
- [x] **C3. Vínculo responsável-criança e regras de criação de conta infantil.** Decisão implementada: somente o adulto cria a família e o primeiro perfil infantil; a criança não se cadastra, entra pelo código da família e pelo próprio PIN. O adulto deve confirmar explicitamente que é responsável e autoriza o tratamento dos dados da criança.

## Fase D — Governança (Prioridade 4)

- [x] **[AQUI] D1. Registro das operações de tratamento** (um por fluxo: cadastro/login adulto, autenticação da criança, tarefas, pontos, crescimento do PACUS, loja/resgates, exclusão de conta) — depende do B1 estar pronto. Ver `docs/REGISTRO_TRATAMENTO.md`.
- [x] **[AQUI] D2. RIPD** (Relatório de Impacto à Proteção de Dados) focado em: dados de crianças, autenticação, isolamento por família, exposição por ObjectId, exclusão de conta, incidentes, dados de comportamento/rotina — depende do B1/D1. Ver `docs/RIPD.md`.
- [x] **[AQUI] D3. Plano de resposta a incidentes** (detecção, investigação, contenção, evidências, correção, avaliação de impacto, comunicação, registro, revisão pós-incidente). Ver `docs/PLANO_RESPOSTA_INCIDENTES.md`.
- [ ] **[DEPOIS] D4. Canal de contato de privacidade** (e-mail/formulário dedicado) — precisa de um endereço/infra que só você pode criar.

## Fase E — CI/infra (achado durante a validação)

- [x] **E1. Consolidar `main` e `feature/next-migration`.** Resolvido em 2026-09-01: `main` foi alinhada ao código de `feature/next-migration`, com o histórico antigo preservado na tag `main-legacy-zip-backup`. Estado verificado em 2026-09-04: `feature/next-migration` (`c23cc10`) é ancestral de `main` (`4c368b7`) e não tem commits pendentes; `main` é a branch de referência e o alvo do CI oficial.

---

### Progresso
_(atualizado a cada item concluído)_

- 2026-08-28/29: Corrigido `TasksController.Delete` sem checagem de família (commit `979cd19`), criado `TasksHttpIntegrationTests.cs`, corrigido `JWT_SECRET` ausente no CI e dois testes desatualizados (regra de pontos antiga). CI validado verde.
- 2026-08-29: **A1 concluído.** Rate limiting nativo do ASP.NET Core (auth: 10/5min por IP, bootstrap: 5/15min por IP), desativado em Development pra não quebrar os testes de integração. Corrigido de brinde um teste com bug de timezone (`HistoryHttpIntegrationTests` calculava "hoje" em UTC em vez de America/Sao_Paulo). CI validado verde.
- 2026-08-29: **A2 concluído.** Adicionados testes de isolamento por família em Store, Habitat, Points, Settings e DailyRoutines (`StoreHttpIntegrationTests.cs`, `HabitatHttpIntegrationTests.cs`, `PointsHttpIntegrationTests.cs`, `SettingsHttpIntegrationTests.cs` novo, `DailyRoutineHttpIntegrationTests.cs`). Nenhum bug encontrado nesses controllers -- todos já escopam por `FamilyId` do token, sem id externo. CI validado verde (commit `d2bb249`). Removido trigger/debug temporário do `ci.yml`.
- 2026-08-29: **A3 concluído.** Adicionados testes de manipulação de ObjectId / troca de papel: `DailyTasksHttpIntegrationTests.TaskOperations_WithAnotherFamilysTaskId_ShouldNotBeAllowed` (id de tarefa de outra família em complete/update/delete -- BadRequest, garantia estrutural via `GetLatestOpenAsync` escopado por FamilyId), `PointsHttpIntegrationTests.AdjustBalance_ShouldBeForbiddenForChild`, `DailyRoutineHttpIntegrationTests.AdjustGameTimer_ShouldBeForbiddenForChild`, e novo arquivo `PacusHttpIntegrationTests.cs` (UpdateState -- controller não tinha nenhuma cobertura ainda: forbidden para criança, isolamento por família). CI validado verde. Removido trigger/debug temporário do `ci.yml`.
- 2026-08-29: **A4 concluído.** Renomeado `UserId` -> `FamilyId` em `DailyRoutine`, `TaskTemplate`, `StoreItem`, `Redemption`, `Settings` e `PointTransaction` (entidades, repositórios e services), com `[BsonElement("userId")]` explícito preservando o nome do campo já gravado no Mongo -- nenhuma migração de dados necessária. DTOs de resposta da API mantidos como estavam (contrato público do frontend não muda). Fora do escopo: `PacusGrowthLog.UserId` e `TaskEvent.UserId` não estavam na lista do item A4. CI validado verde de primeira. Removido trigger temporário do `ci.yml`.
- 2026-08-29: **A5 concluído.** Nova coleção `audit_logs` (entidade `AuditLog` + `IAuditLogRepository`), separada do dado em si, cobrindo as três ações do checklist: exclusão de tarefa permanente (`task_template.deleted`), aprovação/rejeição de resgate (`redemption.approved`/`redemption.rejected`) e ajuste manual de saldo (`points.manual_adjustment`) -- cada entrada registra FamilyId, ActorId, ActorRole, ação, entidade afetada e detalhe legível. Testes de integração novos verificam que cada ação cria a entrada correspondente. CI validado verde de primeira. Removido trigger temporário do `ci.yml`.
- 2026-08-29: **B1 concluído.** Criado `docs/DATA_MAP.md` cobrindo as 12 collections reais do MongoDB (as 11 do checklist + `audit_logs`, criada no A5) — campo a campo, com finalidade, categoria do titular, origem, quem acessa, base legal (LGPD art. 7º/14º), retenção, destino em exclusão e controles de segurança. Achados: `pacus_growth.userId` e `task_events.userId` não foram renomeados no A4 (fora do escopo original -- anotado para revisão futura); nenhum dado sensível (art. 5º, II) é coletado hoje; decisão de retenção/exclusão de `audit_logs` deixada pendente para o B3. Base pronta para B2, B3, D1 e D2.
- 2026-08-29: **B2 concluído.** Novo `GET /api/v1/export` (restrito a adulto), reunindo as 12 collections do `docs/DATA_MAP.md` num único JSON -- membros da família (sem `passwordHash`/`pinHash`), PACUS, habitat, settings, rotinas diárias, templates de tarefa, transações de pontos, logs de crescimento, eventos de tarefa, itens da loja, resgates e logs de auditoria. Novos métodos `GetAllByFamilyAsync` (sem os filtros de "só ativo"/"só recente" da UI normal) em cada repositório. Optei por só JSON -- a estrutura tem arrays aninhados (ex. `tasks` dentro de cada `daily_routine`) que não cabem bem em CSV tabular sem achatar/perder informação; JSON já é legível e é o formato que uma eventual ferramenta de importação usaria. Testes de integração novos: adulto exporta e vê os dados certos, nunca vaza hash de senha/PIN, criança recebe 403, sem token recebe 401, isolamento por família. `docs/DATA_MAP.md` atualizado (achado #2 revisado). CI validado verde de primeira. Removido trigger temporário do `ci.yml`.
- 2026-08-29: **B3 concluído.** Novo `DELETE /api/v1/account` (restrito a adulto, exige a senha atual no corpo -- reautenticação simples contra sessão esquecida/token vazado, já que a operação é irreversível). Estratégia por collection, direto da tabela de retenção do `docs/DATA_MAP.md`: hard delete nas 11 collections (`users`, `pacus`, `habitats`, `settings`, `daily_routines`, `task_templates`, `point_transactions`, `pacus_growth`, `task_events`, `store_items`, `redemptions`); `audit_logs` é a exceção -- anonimizado em vez de apagado (remove o vínculo `ActorId`, mantém ação/entidade/detalhe/data) e recebe `purgeAt` = agora + 12 meses, com índice TTL no Mongo (`expireAfterSeconds: 0`) fazendo a purga definitiva sozinha depois disso; decisão baseada em legítimo interesse (art. 7º, IX) para responsabilização, equilibrado com minimização de dados. A própria ação de exclusão gera uma entrada de auditoria (`account.deleted`), já anonimizada junto com as demais. Novos métodos `DeleteAllByFamilyAsync`/`DeleteByFamilyIdAsync`/`AnonymizeByFamilyAsync` em cada repositório. Testes de integração novos: apaga os dados da família (verificado direto no Mongo), anonimiza (não apaga) `audit_logs`, rejeita senha errada (401, conta intacta), proíbe criança (403), exige token (401), isolamento entre famílias. CI validado verde de primeira. Removido trigger temporário do `ci.yml`.
- 2026-08-29: **B4 concluído.** Rascunhos de `docs/POLITICA_PRIVACIDADE.md` e `docs/TERMOS_DE_USO.md`, escritos com base no funcionamento real do app (README, `docs/DATA_MAP.md`, e o que foi implementado em A1-A5/B1-B3): finalidades e bases legais por categoria de dado, seção de dados de crianças (art. 14), direitos do titular (art. 18) já mapeados para os endpoints reais (export = portabilidade, exclusão de conta = eliminação), e a regra de retenção/anonimização de audit_logs do B3. Marcados com 🔲 os pontos que precisam de uma decisão/preenchimento seu antes de publicar: nome legal completo do responsável (usei só "Pedro"), se e como notificar usuários de mudanças nos termos, e a seção de limitação de responsabilidade (que envolve renúncia/limitação de direitos e pede validação jurídica antes de valer). **Não revisado por advogado** -- ambos os documentos dizem isso explicitamente no topo. Este item não envolve código, então não passou pelo pipeline de CI.
- 2026-08-29: **C1 concluído.** Revisão campo a campo de `users`: `_id`, `role`, `name`, `familyId`, `createdAt`/`updatedAt`, `email`/`passwordHash` (adulto) e `pinHash` (criança) são todos necessários (estruturais ou o próprio mecanismo de autenticação). Achado: `timezone` é gravado sempre como `"America/Sao_Paulo"` no bootstrap, não há endpoint para alterar, e os quatro pontos do código que calculam o dia operacional (`DailyRoutinesController`, `PointsController`, `StoreService`, mais o cálculo geral) ignoram o valor salvo e usam a mesma string fixa -- o campo nunca é lido de volta do banco. Não é bug novo (já havia `TODO` no código sinalizando isso), mas é uma coleta sem função hoje. Documentei as duas saídas possíveis (implementar de verdade via `settings`, ou remover até ser priorizado) em `docs/DATA_MAP.md`, sem remover o campo nesta auditoria -- é decisão de produto, não falha de segurança. Nenhum outro campo problemático encontrado; nenhum dado sensível (art. 5º, II) coletado.
- 2026-08-29: **C2 concluído.** Os testes de isolamento por família existentes (A2/A3/B2/B3) quase todos usam o ADULTO da segunda família para tentar o ataque de id -- faltava a variante com a CRIANÇA nos dois endpoints onde ela tem acesso estrutural igual ao adulto (sem `[RequireRole]`): `DailyTasksHttpIntegrationTests.TaskOperations_WithAnotherFamilysTaskId_AsChild_ShouldNotBeAllowed` (criança da Família B tentando completar/editar/excluir uma tarefa com id da Família A -- 400 nos três) e `StoreHttpIntegrationTests.RequestRedemption_WithAnotherFamilysStoreItemId_AsChild_ShouldNotBeAllowed` (criança da Família B tentando resgatar um item de loja com id da Família A -- 400). `HistoryController` ficou de fora de propósito: o endpoint nunca aceita id externo, sempre escopa por `FamilyId` do token, então não há superfície de ataque por id ali para nenhum papel. Nenhum bug encontrado -- os dois testes novos confirmam que a checagem de posse por `FamilyId` (já existente) protege igualmente adulto e criança. CI validado verde de primeira. Removido trigger temporário do `ci.yml`.
- 2026-08-29: **D1 concluído.** Criado `docs/REGISTRO_TRATAMENTO.md` (registro das operações de tratamento, LGPD art. 37), reorganizando o `docs/DATA_MAP.md` (que é por collection) numa visão por fluxo, como o item pede: os 7 fluxos indicados no checklist mais 2 adicionais que também são operações de tratamento e ficariam de fora se eu seguisse só a lista literal -- exportação de dados (B2) e logs de auditoria/segurança (A1+A5). Cada fluxo tem finalidade, base legal, collections envolvidas, quem opera, compartilhamento com terceiros (nenhum em todos os 9), retenção/eliminação e medidas de segurança. Base pronta para o D2 (RIPD).
- 2026-08-29: **D2 concluído.** Criado `docs/RIPD.md` (Relatório de Impacto à Proteção de Dados), cobrindo as sete áreas indicadas no checklist. Cinco delas (autenticação, isolamento por família, exposição por ObjectId, exclusão de conta, dados de comportamento/rotina) já têm mitigação técnica implementada e testada, com risco residual baixo. As outras duas apontam para itens que este relatório não fecha sozinho: dados de crianças aponta para o C3 (`[DEPOIS]`, decisão de produto sobre o fluxo de cadastro/consentimento) e incidentes de segurança aponta para o próprio D3, feito em seguida. Nenhuma das sete áreas indicou risco alto não mitigado.
- 2026-08-29: **D3 concluído.** Criado `docs/PLANO_RESPOSTA_INCIDENTES.md`, dimensionado para o estágio atual (um responsável técnico, sem equipe de segurança dedicada): o que conta como incidente com exemplos concretos do PACUS, detecção (log de auditoria, relato de usuário, logs de hospedagem -- sem monitoramento automatizado ainda, lacuna anotada), investigação, contenção por tipo de causa (credencial vazada, bug de isolamento, acesso indevido à infra), correção com teste de regressão obrigatório, avaliação de impacto e obrigação de comunicação à ANPD/titulares (art. 48), registro, e revisão pós-incidente. Marcado com 🔲 o que depende de decisão sua: canal de comunicação a titulares (pré-requisito prático é o D4, `[DEPOIS]`), e se/quando nomear um Encarregado de Dados formal (art. 41) se a base de usuários crescer.
- 2026-08-29: **E1 investigado, decisão adiada.** Achado confirmado direto no GitHub: `main` tem 2 commits, o segundo é um `pacus.zip` de 22MB enviado pela interface web, sem histórico compartilhado com `feature/next-migration` (68 commits, código real). Recomendei force-push da `main` (o zip não vale preservar) como alternativa a um merge com `--allow-unrelated-histories` (mantém o zip para sempre, sem ganho). Perguntei antes de mexer na `main` -- é destrutivo e a instrução permanente desta sessão é nunca tocar `main` diretamente sem autorização explícita. Você decidiu não mexer por enquanto; fica documentado aqui para quando quiser retomar.
- 2026-09-01: **E1 concluído.** Autorização explícita recebida ("Vamos consolidar a branch main, sem perder a feature/next-migration"), com escolha confirmada entre as duas opções: force-push (recomendado). Antes de mexer na `main`, criei e enviei ao GitHub a tag `main-legacy-zip-backup` apontando pro estado antigo dela (os 2 commits do zip ficam recuperáveis por ali, se um dia fizer falta). Depois, force-push de `main` para o commit de `feature/next-migration` -- `main` agora tem o app inteiro. `ci.yml` só dispara em push para `main`, então esse push já rodou o CI oficial de verdade (não o trigger temporário) e passou verde: 85 testes (unitários + integração com MongoDB via Testcontainers), backend e frontend. A partir de agora, dar push em `main` valida tudo automaticamente de novo -- não precisa mais do truque de trigger temporário só pra revalidar `feature/next-migration`.
